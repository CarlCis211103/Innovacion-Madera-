// main.js — punto de entrada. Importa aquí los módulos según se vayan creando.
// Ejemplo futuro: import { initHeroAnimation } from "./modules/animations.js";

document.addEventListener("DOMContentLoaded", () => {
  // Inicialización general del sitio.
});
